# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
*.swp
*.swo
*~
*.bak

# VSCode (keep settings.json — it has project-level eol/linter config)
.vscode/launch.json
.vscode/tasks.json
.vscode/*.code-workspace
.vscode/extensions.json

# Logs and temp files (these live on pfSense, not in the repo)
*.log
*.tmp
*.txt.bz2

# AI/dev tool instructions — local only, not for public repo
.github/instructions/

# Sensitive — never commit the webhook config
geoblock-webhook.conf
*.conf
